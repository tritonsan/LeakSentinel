"""Amazon Bedrock (Nova) integration helpers.

This package is optional at runtime: local mode works without boto3/AWS creds.
Bedrock mode will gracefully fall back to local heuristics if AWS isn't configured.
"""

